__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg
.env
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.db
*.sqlite3
.DS_Store
node_modules/
credentials.json
service-account*.json

# SMS consent audit log (contains personal data — never commit)
data/consent/

# Internal audit docs
docs/audit/

# Submission and demo working material: kept locally, not published.
# These are drafts and run-sheets for one hackathon entry, not part of the
# system. Anyone cloning the repo wants the code and the pillar docs, not a
# beat sheet with a phone number in it.
docs/DEVPOST_SUBMISSION.md
docs/A2P_10DLC_RESUBMISSION.md
docs/DEMO_SCRIPT_4MIN.md
docs/DEMO_SEQUENCE.md

# Everything under stage3 is bonus-round material: the dev.to draft, the build
# log it was adapted from, and the social drafts. Ignoring the directory rather
# than the three files means a fourth draft written later is covered too.
docs/stage3/
